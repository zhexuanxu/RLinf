# Minimal StarVLA runtime dependencies for RLinf StarVLA wrapper.
# Keep these pins to avoid known compatibility breakages:
# - transformers requires huggingface-hub < 1.0
# - newer diffusers versions require much newer peft than RLinf embodied stack
accelerate==1.5.2
diffusers==0.33.1
einops
huggingface-hub==0.36.2
numpy==1.26.4
omegaconf
peft==0.11.1
# Used by some Qwen-VL processing utilities (imported transitively in some setups).
qwen-vl-utils
safetensors
tiktoken
transformers==4.57.6
transformers_stream_generator==0.0.4
