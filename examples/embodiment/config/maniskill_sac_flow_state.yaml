defaults:
  - env/maniskill_pick_cube@env.train
  - env/maniskill_pick_cube@env.eval
  - model/mlp_policy@actor.model
  - training_backend/fsdp@actor.fsdp_config
  - override hydra/job_logging: stdout

hydra:
  run:
    dir: .
  output_subdir: null
  searchpath:
    - file://${oc.env:EMBODIED_PATH}/config/

cluster:
  num_nodes: 1
  component_placement: 
    actor,env,rollout: 0

runner:
  task_type: embodied
  logger:
    log_path: "../results"
    project_name: rlinf
    experiment_name: "sac_flow_state"
    logger_backends: ["tensorboard"] # wandb, swanlab

  max_epochs: 8000
  max_steps: -1

  only_eval: False
  val_check_interval: 100
  save_interval: -1

algorithm:
  update_epoch: 32
  normalize_advantages: False  # SAC doesn't use advantages
  kl_penalty: kl
  group_size: 1

  agg_q: min  # agg_q for critic
  actor_agg_q: min # agg_q for actor, SAC uses min while RLPD uses mean
  critic_subsample_size: 2 # number of Q-heads to subsample for critic update, used only in RLPD (num_q_heads=10)
  enable_drq: False

  rollout_epoch: 1
  eval_rollout_epoch: 1 # same as maniskill_sac_mlp.yaml

  reward_type: action_level
  logprob_type: action_level
  entropy_type: action_level

  adv_type: embodied_sac  # Use SAC returns computation
  loss_type: embodied_sac  # Use SAC loss functions with Gumbel-Softmax
  loss_agg_func: "token-mean"

  bootstrap_type: always # same as maniskill_sac_mlp.yaml
  # SAC hyperparameters
  gamma: 0.8 # 0.96
  tau: 0.005  # Soft update coefficient for target networks

  entropy_tuning:
    alpha_type: softplus # choose from ["softplus", "exp", "fixed_alpha"], set to "softplus" or "exp" to enable automatic entropy tuning
    initial_alpha: 0.2  # Initial temperature value
    target_entropy: -4  # Target entropy (-action_dim)
    optim:
      lr: 3.0e-4  # Learning rate for temperature parameter
      lr_scheduler: torch_constant
      clip_grad: 10.0

  critic_actor_ratio: 3
  backup_entropy: True
  
  # Replay buffer settings
  replay_buffer:
    enable_cache: True
    cache_size: 6000 # number of trajectories cached in memory
    sample_window_size: 6000 # number of latest trajectories to sample from for replay buffer
    min_buffer_size: 2  # Minimum buffer size before training starts (in number of trajectories)
    auto_save: False

  num_updates_per_step: 64  # Number of gradient updates per training step
  target_update_freq: 1  # Frequency of target network updates

  # params for rollout
  sampling_params:
    do_sample: True
    temperature_train: 1.0
    temperature_eval: 0.6
    top_k: 50
    top_p: 1.0
    repetition_penalty: 1.0

  # length argument for autoregressive sampling
  length_params:
    max_new_token: 7
    max_length: 1024
    min_length: 1

env:
  group_name: "EnvGroup"

  train:
    total_num_envs: 32
    auto_reset: True
    ignore_terminations: True
    use_fixed_reset_state_ids: False
    max_episode_steps: 50 # max episode steps for truncation
    max_steps_per_rollout_epoch: 2
  
  eval:
    total_num_envs: 16
    auto_reset: True
    ignore_terminations: True
    max_episode_steps: 50
    max_steps_per_rollout_epoch: 50
    use_fixed_reset_state_ids: True
    group_size: 1
    video_cfg:
      save_video: True
      video_base_dir: ${runner.logger.log_path}/video/eval

rollout:
  group_name: "RolloutGroup"
  backend: "huggingface"
  enable_offload: False
  pipeline_stage_num: 1

  collect_transitions: True
  collect_prev_infos: False

  model:
    model_path: ""
    precision: ${actor.model.precision}

actor:
  group_name: "ActorGroup"
  training_backend: "fsdp"

  micro_batch_size: 512  # Smaller batch size for SAC. 1024 can also be considered
  global_batch_size: 512   # Smaller global batch size
  seed: 1234
  enable_offload: False

  model:
    model_type: "flow_policy"
    input_type: "state" # ['state', 'mixed']
    policy_setup: "panda-ee-dpos"
    obs_dim: 42
    action_dim: 4
    num_action_chunks: 1
    hidden_dim: 256
    
    precision: "32"
    add_value_head: False
    add_q_head: True
    num_q_heads: 2  # 2 for SAC, 10 for RLPD
    
    # Flow Matching specific parameters
    denoising_steps: 4  # Number of denoising steps for flow matching
    d_model: 96  # Transformer model dimension
    n_head: 4  # Number of attention heads
    n_layers: 2  # Number of transformer layers
    use_batch_norm: False  # Whether to use batch normalization
    batch_norm_momentum: 0.99  # Batch normalization momentum
    flow_actor_type: "JaxFlowTActor"  # "JaxFlowTActor" or "FlowTActor". "JaxFlowTActor" supports the following noise std settings:
    noise_std_head: True  # Whether to use a separate head to predict noise_std, otherwise fixed std is used
    log_std_min_train: -5  # Min log std for training (if using noise_std_head)
    log_std_max_train: 2   # Max log std for training (if using noise_std_head)
    log_std_min_rollout: -10  # Min log std for rollout (if using noise_std_head)
    log_std_max_rollout: 1    # Max log std for rollout (if using noise_std_head)
    noise_std_train: 0.3  # Fixed noise std for training (if not using noise_std_head)
    noise_std_rollout: 0.02  # Fixed noise std for rollout (if not using noise_std_head)
    
    is_lora: False
    lora_rank: 32
    ckpt_path: null
    gradient_checkpointing: False
    sharding_strategy: no_shard
    vh_mode: "q_network"
    q_network_separate_processing: True

  optim:
    clip_grad: 1.0 # 10.0 can also be considered
    lr: 3e-4
    lr_scheduler: torch_constant

  critic_optim:
    clip_grad: 1.0 # 10.0 can also be considered
    lr: 3e-4
    lr_scheduler: torch_constant

  # Override the default values in training_backend/fsdp
  fsdp_config:
    strategy: "fsdp"
    sharding_strategy: "no_shard"
    mixed_precision:
      param_dtype: ${actor.model.precision}
      reduce_dtype: ${actor.model.precision}
      buffer_dtype: ${actor.model.precision}

reward:
  use_reward_model: False

critic:
  use_critic_model: False

