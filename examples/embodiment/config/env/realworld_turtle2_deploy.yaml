env_type: realworld

total_num_envs: 1

auto_reset: False
ignore_terminations: False
reward_mode: raw
wrap_obs_mode: simple
seed: 0
group_size: 1
use_fixed_reset_state_ids: False
max_steps_per_rollout_epoch: 300
max_episode_steps: 300
no_gripper: False
main_image_key: wrist_2

video_cfg:
  save_video: False
  info_on_video: True
  video_base_dir: ${runner.logger.log_path}/video/eval

init_params:
  id: "Turtle2DeployEnv-v1"
  num_envs: null

override_cfg:
  is_dummy: false
  action_mode: relative_pose
  task_description: "Describe your Turtle2 deployment task here."
  use_arm_ids: [0, 1]
  use_camera_ids: [0, 1, 2]
  enforce_gripper_close: false
  # The default deploy env keeps Turtle2's relative-pose action semantics.
  # Set action_mode: absolute_pose when the policy emits absolute
  # [x, y, z, rx, ry, rz, gripper] commands per arm.
  reset_ee_pose:
    - [0.20, 0.00, 0.10, 0.00, 0.00, 0.00]
    - [0.20, 0.00, 0.10, 0.00, 0.00, 0.00]
  max_num_steps: 300
  ee_pose_limit_min:
    - [-0.20, -0.60, -0.05, -3.20, -3.20, -3.20]
    - [-0.20, -0.60, -0.05, -3.20, -3.20, -3.20]
  ee_pose_limit_max:
    - [0.80, 0.60, 0.60, 3.20, 3.20, 3.20]
    - [0.80, 0.60, 0.60, 3.20, 3.20, 3.20]
