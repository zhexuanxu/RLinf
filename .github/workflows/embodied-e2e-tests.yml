name: Embodied End-to-End Tests

on:
  workflow_call:

jobs:
  embodied-mlp-sac-maniskill-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          bash requirements/install.sh embodied --model openvla --env maniskill_libero

      - name: ManiSkill SAC test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh maniskill_sac_mlp

      - name: ManiSkill Async SAC test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run_async.sh maniskill_sac_mlp_async

      - name: ManiSkill SAC Flow State test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh maniskill_sac_flow_state

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-d4rl-offline-iql-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment (d4rl)
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh embodied --env d4rl

      - name: D4RL Offline IQL test (no FSDP wrap)
        timeout-minutes: 15
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run_offline.sh d4rl_iql_mujoco

      - name: D4RL Offline IQL test (FSDP wrap)
        timeout-minutes: 15
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run_offline.sh d4rl_iql_mujoco egl actor.use_fsdp_wrap=true

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-cnn-realworld-dummy-sac-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          bash requirements/install.sh embodied --model openvla --env maniskill_libero

      - name: ManiSkill SAC test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh realworld_dummy_sac_cnn

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-cnn-realworld-xsquare-turtle2-dummy-sac-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          bash requirements/install.sh embodied --model openvla --env maniskill_libero

      - name: Xsquare-turtle2 SAC dummy test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh realworld_xsquare_turtle2_dummy_sac_cnn

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-mlp-realworld-dosw1-dummy-sac-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh embodied --env dosw1

      - name: DOSW1 Pick SAC dummy test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh dosw1_dummy_sac_mlp_pick

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-mlp-frankasim-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export SERL_PATH=/workspace/dataset/serl
          bash requirements/install.sh embodied --model openvla --env frankasim

      - name: MLP PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh frankasim_ppo_mlp

      - name: CNN SAC test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run_async.sh frankasim_sac_cnn_async

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvla-maniskill-libero-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          bash requirements/install.sh embodied --model openvla --env maniskill_libero

      - name: ManiSkill PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh maniskill_ppo_openvla

      - name: ManiSkill Async PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run_async.sh maniskill_async_ppo_openvla

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-maniskill-libero-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          bash requirements/install.sh embodied --model openvla-oft --env maniskill_libero

      - name: ManiSkill GRPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cp -r /workspace/dataset/maniskill_assets/assets ${REPO_PATH}/rlinf/envs/maniskill/
          bash tests/e2e_tests/embodied/run.sh maniskill_grpo_openvlaoft

      - name: Libero-Goal GRPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_goal_grpo_openvlaoft

      - name: ManiSkill Async PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cp -r /workspace/dataset/maniskill_assets/assets ${REPO_PATH}/rlinf/envs/maniskill/
          bash tests/e2e_tests/embodied/run_async.sh maniskill_async_ppo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-behavior-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export BEHAVIOR_PATH=/workspace/dataset/BEHAVIOR-1K
          export ISAAC_SIM_WHEEL_PATH=/workspace/dataset/isaac_sim_wheels
          bash requirements/install.sh embodied --model openvla-oft --env behavior

      - name: BEHAVIOR-OpenVLA-OFT test
        timeout-minutes: 30
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export OMNIGIBSON_DATA_PATH=/workspace/dataset/behavior-datasets
          export ISAAC_PATH=/workspace/dataset/isaac-sim
          bash tests/e2e_tests/embodied/run.sh behavior_ppo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune
          rm -rf /tmp/tmp*

  embodied-openvlaoft-robotwin-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh embodied --model openvla-oft --env robotwin

      - name: RoboTwin-OpenVLA-OFT test
        timeout-minutes: 30
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export ROBOT_PLATFORM=ALOHA
          export ROBOTWIN_PATH="/workspace/dataset/RoboTwin"
          export PYTHONPATH=${ROBOTWIN_PATH}:$PYTHONPATH
          bash tests/e2e_tests/embodied/run.sh robotwin_grpo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openpi-behavior-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export BEHAVIOR_PATH=/workspace/dataset/BEHAVIOR-1K
          export ISAAC_SIM_WHEEL_PATH=/workspace/dataset/isaac_sim_wheels
          bash requirements/install.sh embodied --model openpi --env behavior

      - name: BEHAVIOR-OpenPI test
        timeout-minutes: 30
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export OMNIGIBSON_DATA_PATH=/workspace/dataset/behavior-datasets
          export ISAAC_PATH=/workspace/dataset/isaac-sim
          bash tests/e2e_tests/embodied/run.sh behavior_ppo_openpi

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune
          rm -rf /tmp/tmp*

  embodied-gr00t-maniskill-libero-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          export GR00T_PATH=/workspace/dataset/Isaac-GR00T/
          bash requirements/install.sh embodied --model gr00t --env maniskill_libero

      - name: Libero-Spatial PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_spatial_ppo_gr00t

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-gr00t-isaaclab-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export ISAAC_LAB_PATH=/workspace/dataset/IsaacLab
          export GR00T_PATH=/workspace/dataset/Isaac-GR00T/
          bash requirements/install.sh embodied --model gr00t --env isaaclab

      - name: ISAACLAB PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          source /workspace/dataset/isaac-sim-5.1/setup_conda_env.sh
          bash tests/e2e_tests/embodied/run.sh isaaclab_ppo_gr00t

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune
          rm -rf /tmp/tmp*

  embodied-openpi-isaaclab-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export ISAAC_LAB_PATH=/workspace/dataset/IsaacLab
          bash requirements/install.sh embodied --model openpi --env isaaclab

      - name: ISAACLAB OpenPI PPO Pi05 test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          source /workspace/dataset/isaac-sim-5.1/setup_conda_env.sh
          bash tests/e2e_tests/embodied/run.sh isaaclab_ppo_openpi_pi05

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune
          rm -rf /tmp/tmp*
          
  embodied-openpi-maniskill-libero-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          bash requirements/install.sh embodied --model openpi --env maniskill_libero

      - name: Maniskill PPO Pi05 test
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cp -r /workspace/dataset/maniskill_assets/assets ${REPO_PATH}/rlinf/envs/maniskill/
          bash tests/e2e_tests/embodied/run.sh maniskill_ppo_openpi05

      - name: Libero-Spatial PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_spatial_ppo_openpi

      - name: Libero-Object NFT test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_object_nft_openpi

      - name: Libero-Spatial DAgger test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_spatial_dagger_openpi

      - name: Libero-Spatial PPO Pi05 test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_spatial_ppo_openpi05

      - name: ManiSkill Async PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cp -r /workspace/dataset/maniskill_assets/assets ${REPO_PATH}/rlinf/envs/maniskill/
          bash tests/e2e_tests/embodied/run_async.sh maniskill_async_ppo_openpi

      - name: Libero-Spatial DSRL PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_spatial_dsrl_openpi

      - name: Maniskill PPO Co-Training Pi05 test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cp -r /workspace/dataset/RLCo-maniskill-assets/custom_assets ${REPO_PATH}/rlinf/envs/maniskill/assets
          bash tests/e2e_tests/embodied/run.sh maniskill_ppo_co_training_openpi_pi05

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openpi-maniskill-gsenv-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          bash requirements/install.sh embodied --model openpi --env maniskill_libero

          source .venv/bin/activate
          cd /workspace/dataset/ManiSkill-GS
          uv pip install -e .
          python scripts/test_rlinf_interface.py

      - name: ManiSkill-GS PPO Pi05 test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh gsenv_ppo_openpi_pi05

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openpi-metaworld-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh embodied --model openpi --env metaworld

      - name: Metaworld PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh metaworld_50_ppo_openpi

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-metaworld-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh embodied --model openvla-oft --env metaworld

      - name: Metaworld PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh metaworld_50_ppo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openpi-calvin-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export CALVIN_PATH=/workspace/dataset/calvin
          bash requirements/install.sh embodied --model openpi --env calvin

      - name: Calvin PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh calvin_ppo_openpi

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-calvin-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export CALVIN_PATH=/workspace/dataset/calvin
          bash requirements/install.sh embodied --model openvla-oft --env calvin

      - name: Calvin PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh calvin_ppo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openpi-robocasa-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export ROBOCASA_PATH=/workspace/dataset/robocasa
          bash requirements/install.sh embodied --model openpi --env robocasa

      - name: Robocasa PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh robocasa_closedrawer_ppo_openpi

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openpi-roboverse-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh embodied --model openpi --env roboverse

      - name: RoboVerse PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          cp -r /workspace/dataset/roboverse_data .
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh roboverse_ppo_openpi_pi05
          mv roboverse_data .venv/roboverse/

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-dexbotic-maniskill-libero-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          export DEXBOTIC_PATH=/workspace/dataset/dexbotic
          bash requirements/install.sh embodied --model dexbotic --env maniskill_libero

      - name: Libero-Spatial PPO Dexbotic test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_spatial_ppo_dexbotic_pi0

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-starvla-maniskill-libero-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          export STARVLA_PATH=/workspace/dataset/starVLA
          bash requirements/install.sh embodied --model starvla --env maniskill_libero

      - name: Libero-10 GRPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh libero_10_grpo_starvla

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-opensora-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          export OPENSORA_PATH=/workspace/dataset/opensora
          bash requirements/install.sh embodied --model openvla-oft --env opensora

      - name: OpenSora GRPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh opensora_libero_spatial_grpo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-wan-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          export WAN_PATH=/workspace/dataset/wan
          bash requirements/install.sh embodied --model openvla-oft --env wan

      - name: Wan GRPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/embodied/run.sh wan_libero_spatial_grpo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-lingbotvla-robotwin-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export ROBOTWIN_PATH=/workspace/dataset/RoboTwin
          export LINGBOT_PATH=/workspace/dataset/lingbot-vla
          bash requirements/install.sh embodied --model lingbotvla --env robotwin

      - name: RoboTwin-LingBotVLA GRPO test
        timeout-minutes: 30
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export ROBOT_PLATFORM=ALOHA
          export ROBOTWIN_PATH="/workspace/dataset/RoboTwin"
          export PYTHONPATH=${ROBOTWIN_PATH}:$PYTHONPATH
          export LINGBOT_VLA_PATH="/workspace/dataset/lingbot-vla"
          bash tests/e2e_tests/embodied/run.sh robotwin_grpo_lingbotvla

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-liberopro-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          export LIBERO_PRO_PATH=/workspace/dataset/LIBERO-PRO
          bash requirements/install.sh embodied --model openvla-oft --env liberopro

      - name: LIBERO-Pro GRPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export LIBERO_TYPE=pro
          bash tests/e2e_tests/embodied/run.sh liberopro_10_grpo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-openvlaoft-liberoplus-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export LIBERO_PATH=/workspace/dataset/LIBERO
          export LIBERO_PLUS_PATH=/workspace/dataset/LIBERO-PLUS
          bash requirements/install.sh embodied --model openvla-oft --env liberoplus

      - name: LIBERO-Plus GRPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export LIBERO_TYPE=plus
          bash tests/e2e_tests/embodied/run.sh liberoplus_10_grpo_openvlaoft

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune

  embodied-embodichain-test:
    runs-on: embodied
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create embodied environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh embodied --env embodichain

      - name: EmbodiChain CartPole PPO test
        timeout-minutes: 20
        run: |
          unset PYTHONPATH
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export EMBODICHAIN_DATA_ROOT=/workspace/dataset/embodichain_data
          python -m embodichain.data download --name CartPole
          python -m embodichain.data download --name SimResources
          bash tests/e2e_tests/embodied/run.sh embodichain_ppo_cart_pole

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune