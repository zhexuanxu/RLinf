没有KI
要开amp_context吗？不是说每一层精度不一样吗
use_orig_params是什么？
tensorboard --logdir logs

curl -fsSL https://claude.ai/install.sh | bash -s 2.1.109
{
  "env": {
    "DISABLE_AUTOUPDATER": "1"
  }
}
export http_proxy="http://127.0.0.1:1080"
export https_proxy="http://127.0.0.1:1080"